import { DriverHome } from '@/components/driver/DriverHome';

/** The driver's one-screen mobile-web app. Auth/RBAC is gated by layout.tsx. */
export default function DriverPage() {
  return <DriverHome />;
}
